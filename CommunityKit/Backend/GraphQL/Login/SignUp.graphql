mutation SignUp($input: SignUpInput!) {
  signUp(input: $input) {
    member{
      id,
      username,
      emailAddress,
      name{
        firstName,
        lastName
      },
      profilePicture,
      admin,
    },
    error
  }
}
